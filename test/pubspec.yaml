name: game_scaffold_test
description: Testing support for game_scaffold.
version: 0.5.0-dev
homepage: https://www.github.com/TimWhiting/game_scaffold

environment:
  sdk: ">=3.13.0 <4.0.0"

resolution: workspace

dependencies:
  game_scaffold_dart: ^0.5.0-dev
  meta: ^1.15.0
  test: ^1.25.8

dev_dependencies:
  game_scaffold_games: ^0.5.0-dev

