name: game_scaffold
description: Flutter support for a turn based game framework.
version: 0.5.0-dev
homepage: https://github.com/TimWhiting/game_scaffold

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.3.0"

dependencies:
  collection: ^1.19.0
  firebase_dart: ^1.2.2
  flutter:
    sdk: flutter
  flutter_hooks: ^0.20.5
  flutter_riverpod: ^2.6.1
  game_scaffold_dart: ^0.5.0-dev
  hooks_riverpod: ^2.6.1
  logging: ^1.3.0
  riverpod: ^2.6.1

dev_dependencies:
  dartdoc: ^8.3.0
  flutter_test:
    sdk: flutter
  game_scaffold_games: ^0.5.0-dev

flutter: null
